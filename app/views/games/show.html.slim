-provide(:title, 'Play Game')
- if !@game.started_at
  = "Waiting for #{@game.remaining_players} #{@game.remaining_players == 1 ? 'player' : 'players'}..."
- else 
  = "#{current_user.name}" 
  = render 'games/play_game'

  