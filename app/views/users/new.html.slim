-provide(:title, 'Create Account')

h1 Create Account

= simple_form_for(@user) do |f| 
  .card--padded
    .card__body 
      = f.input :name
      = f.input :email
      = f.input :password
      = f.input :password_confirmation
    .card__footer
      .flex.justify-end
        = f.submit "Create my account", class: "btn-primary"








